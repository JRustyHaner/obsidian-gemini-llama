// Floating fallback notice intentionally disabled.
// The project now uses the Agent View tool-execution UI for provider fallback
// messages. Keep this no-op export for compatibility with any remaining imports.

export function showFallbackNotice(): void {
	// no-op to avoid floating popups
}
